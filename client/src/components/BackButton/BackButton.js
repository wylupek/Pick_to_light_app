import React from 'react';
import './BackButton.scss';

const BackButton = () => {
    return (
        <button className="back-button">
            WRÓĆ
        </button>
    );
}

export default BackButton;
